spring:
  application:
    name: HMPT
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pt?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    # Druid settings
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1 FROM DUAL
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true

# File Storage Configuration
aliyun:
  oss:
    endpoint: https://oss-cn-beijing.aliyuncs.com
    accessKeyId: ${OSS_ACCESS_KEY_ID:your_access_key_id}
    accessKeySecret: ${OSS_ACCESS_KEY_SECRET:your_access_key_secret}
    bucketName: ${OSS_BUCKET_NAME:your_bucket_name}

minio:
  endpoint: ${MINIO_ENDPOINT:https://127.0.0.1:9000/}
  accessKey: ${MINIO_ACCESS_KEY:1GfxsoIpxnHovynsf13R}
  secretKey: ${MINIO_SECRET_KEY:Qb1EPjGKlJWMsPCIArIQvMYWJT9a4Xlw1z9IIpEu}
  bucketName: ${MINIO_BUCKET_NAME:test1}

# JWT Configuration
jwt:
  signKey: ${JWT_SIGN_KEY:itheima}
  expire: ${JWT_EXPIRE:43200000} # 12 hours

